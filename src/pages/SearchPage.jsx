import { useMemo, useState } from 'react';
import { useApp } from '../context/AppContext';
import ProductCard from '../components/ProductCard';
export default function SearchPage(){const { products }=useApp(); const [q,setQ]=useState(''); const results=useMemo(()=>products.filter(p=>`${p.name} ${p.category} ${p.collection}`.toLowerCase().includes(q.toLowerCase())),[q,products]); return <main className="pt-[var(--site-header-h,68px)] min-h-screen"><section className="max-w-screen-2xl mx-auto px-6 lg:px-12 py-16"><h1 className="font-display font-black text-5xl uppercase mb-8">Search</h1><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products, categories, collections…" className="up-input text-xl mb-12"/>{q&&<p className="text-xs text-muted uppercase tracking-widest mb-7">{results.length} results</p>}<div className="grid grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-12">{results.map(p=><ProductCard key={p.id} product={p}/>)}</div></section></main>}
